# Vast / GPU steering stack.
# Prefer the frozen lock (torch/torchvision/CUDA + transformers):
#   python scripts/install_vast_env.py
#   # lock: scripts/env/vast_steering_cu121.json
#
# Loose fallback (PIN_VAST_ENV=0):
#   python -m pip install -r scripts/requirements-vast.txt
#
# Root requirements.txt may say numpy>=2 — do NOT use that on Vast.
numpy>=1.26,<2
transformers>=4.50
accelerate
huggingface_hub
python-dotenv
PyYAML
scipy
pandas
pyarrow
# Qwen3.5 kernels: faster path. causal-conv1d is NOT a hard dep —
# building it from source fails on CUDA 12.x images (wheel looks for cu13/torch 2.14).
# ensure_steering_env installs FLA best-effort and skips causal-conv1d source builds;
# missing causal-conv1d → slow PyTorch fallback.
flash-linear-attention
# torch/torchvision: use lock (2.5.1+cu121 / 0.20.1). Do not pip-install bare torchvision
# from PyPI — ABI mismatch breaks Qwen3.5 import.
# torchaudio: uninstall (text-only).
